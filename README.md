# Радар конкурентов

Программа, которая каждый день заходит на сайты конкурентов, запоминает, что там
написано, и сравнивает с тем, что было вчера. Если что-то изменилось — сообщает.

Раз в неделю, по понедельникам, владелец получает в Telegram одно сообщение: что
поменялось у конкурентов за неделю, по каждому игроку отдельно, со ссылкой на
страницу. Если у кого-то изменилась цена или появился новый тариф — сообщение
приходит сразу, не дожидаясь понедельника.

Рынок — речевая аналитика в России, 19 конкурентов. Семнадцать из них программа
обходит сама, двоих приходится смотреть глазами. Почему — в конце этого файла.

## Как это устроено, если совсем коротко

Со страницы берётся не весь её код, а только тот текст, который видит человек:
заголовки, описания, цены. Этот текст сохраняется в файл — дальше такой файл
называется **снимком**. Завтра делается такой же снимок и сравнивается со
вчерашним. Разница между ними и есть изменение.

Нейросети в этом нет. Сравнение двух текстов — обычная операция: она ничего не
стоит, работает одинаково каждый раз и не выдумывает того, чего на странице нет.
Искусственный интеллект нужен только на последнем шаге — объяснить человеку,
какие из сорока найденных изменений важны. Это делается в обычном чате с
ассистентом, отдельный платный доступ для этого не нужен.

## Что читать и в каком порядке

| Файл | О чём |
|---|---|
| этот README | Как поставить, как запустить, где что лежит |
| [FAZA-0-OTCHET.md](FAZA-0-OTCHET.md) | Что показала проверка источников: какие страницы у конкурентов есть, каких нет, где подстелены ловушки |
| [FAZA-1-OTCHET.md](FAZA-1-OTCHET.md) | Как закрыт доступ к ВКонтакте от утечки и чем это проверено |
| [FAZA-2-OTCHET.md](FAZA-2-OTCHET.md) | Как устроен ежедневный сбор и что показал первый боевой прогон |
| [FAZA-3-OTCHET.md](FAZA-3-OTCHET.md) | Как радар находит разницу между снимками и почему порог именно такой |
| [FAZA-4-OTCHET.md](FAZA-4-OTCHET.md) | Как радар решает, что срочно, и как устроены сообщения |
| [FAZA-5-OTCHET.md](FAZA-5-OTCHET.md) | Как радар встал на расписание и как замечает, что перестал собирать |
| [FAZA-6-OTCHET.md](FAZA-6-OTCHET.md) | Неделя обкатки: как порог и шумодав выбираются по живым данным |
| [OBKATKA.md](OBKATKA.md) | Что делать в эту неделю: пять минут в день и чем её закрыть |
| [VK-TOKEN.md](VK-TOKEN.md) | Как получить доступ к ВКонтакте, если понадобится заново |
| [TELEGRAM-BOT.md](TELEGRAM-BOT.md) | Как завести бота радара и куда положить его токен |
| [RASPISANIE.md](RASPISANIE.md) | Как поставить ежедневный запуск, проверить его и снять |
| [sources.yaml](sources.yaml) | Список всего, что обходим: адреса страниц и каналов |
| [config.yaml](config.yaml) | Настройки, которые задаёт человек. Секретов там нет |
| [noise.yaml](noise.yaml) | Что на страницах меняется само по себе и потому не считается изменением |
| [rules.yaml](rules.yaml) | Что считается срочным: слова рынка, по которым сообщение уходит сразу |
| [план работ](../Радар%20конкурентов%20—%20контекст%20для%20разработки/04-tehnicheskij-plan.md) | Все решения по проекту и причина каждого |

## На каком этапе проект

Работа разбита на семь фаз — это просто этапы, идущие по порядку. Так они
названы в плане, и так же названы файлы отчётов.

| Фаза | Что это | Состояние |
|---|---|---|
| 0 | Проверить, что источники вообще отдаются | Сделано 18.08.2026: 49 страниц, 11 каналов в соцсетях |
| 1 | Собрать каркас проекта | Сделано 18.08.2026 |
| 2 | Научиться снимать и сохранять страницы | Сделано 19.08.2026: обход 60 источников за 3 минуты, 0 ошибок |
| 3 | Научиться находить разницу между снимками | Сделано 19.08.2026: разбор построчный, цены разбираются отдельно |
| 4 | Отделить важное от обычного и слать в Telegram | Сделано 19.08.2026: срочное уходит сразу, обычное — сводкой в понедельник |
| 5 | Поставить на расписание | Сделано 20.08.2026: прогон в 11:37, тревога на второй день без сбора |
| 6 | Неделя обкатки на живых данных перед боевым запуском | Идёт с 20.08.2026, закрывается 27.08.2026 |

Шестая фаза выглядит необязательной, но пропускать её нельзя. Без недели обкатки
система заваливает владельца мелочами, и через две недели её перестают читать.

Пока она идёт, радар работает как обычно, но его сообщения помечены шапкой
«ОБКАТКА» и боевой рассылкой не считаются. От человека нужно пять минут в день:
сказать про каждую находку одно слово — сигнал это или шум. Что именно делать —
в [OBKATKA.md](OBKATKA.md).

## Как поставить

Нужен Python версии 3.12 или новее. Дальше три команды:

```
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

Первая создаёт отдельную папку под библиотеки, чтобы они не смешивались с тем,
что уже стоит на компьютере. Вторая её включает. Третья ставит четыре библиотеки,
которые нужны проекту.

Проверено на Python 3.12.4 под Windows. Версии библиотек закреплены — через год
всё поставится ровно таким же, каким работает сейчас.

## Что где лежит

```
radar/
├── collect.py             ежедневный сбор: снять страницы и сохранить снимки
├── detect.py              поиск изменений: сравнить сегодняшний снимок со вчерашним
├── daily.py               прогон целиком: сбор, разбор, уведомления, сводка, здоровье
├── notify.py              срочность и отправка: что слать сразу, что копить до сводки
├── config.yaml            настройки: почтовый ящик, подпись бота, правила сбора, разбора и рассылки
├── sources.yaml           что обходим: 19 конкурентов, 49 страниц, 11 каналов
├── noise.yaml             что на страницах меняется само и не считается изменением
├── rules.yaml             что считается срочным: слова рынка и правило первого экрана
├── requirements.txt       список библиотек
├── README.md              этот файл
├── FAZA-0-OTCHET.md       что показала проверка источников
├── FAZA-1-OTCHET.md       каркас проекта и защита доступа к ВКонтакте
├── FAZA-2-OTCHET.md       как работает сбор и что показал первый прогон
├── FAZA-3-OTCHET.md       как находится разница и почему порог именно такой
├── FAZA-4-OTCHET.md       как радар решает, что срочно, и что при этом ломалось
├── FAZA-5-OTCHET.md       как радар встал на расписание и как замечает своё молчание
├── FAZA-6-OTCHET.md       неделя обкатки: чем калибруется порог и что она уже нашла
├── OBKATKA.md             что делать в неделю обкатки и как её закрыть
├── VK-TOKEN.md            как получить доступ к ВКонтакте
├── TELEGRAM-BOT.md        как завести бота и куда положить его токен
├── RASPISANIE.md          как поставить ежедневный запуск, проверить и снять
├── vk_token.py            программа получения и обновления этого доступа
├── vk_tokens.json         сам доступ к ВК. Секрет, наружу не отдаётся
├── telegram_tokens.json   доступ к боту. Секрет, наружу не отдаётся
├── telegram_tokens.example.json   образец без секретов, его можно копировать
├── data/
│   ├── phase0-*.json      результаты проверки источников по шагам
│   └── _raw/              101 сохранённая страница, 45 МБ. Наружу не отдаётся
├── snapshots/             снимки страниц по дням: <домен>/<страница>/<дата>.txt
├── runs/                  отчёт о каждом дневном обходе: <дата>.json
├── diffs/                 найденные изменения: <домен>/<страница>/<дата>.json
│                          и сводка дня <дата>.json. Не чистится никогда
├── notify/                что и когда отправлено человеку: журнал и отчёты по дням
├── calibration/           неделя обкатки: лист разметки и отчёты калибровки
├── logs/                  вывод ежедневных прогонов: <дата>.log. Чистится через 90 дней
└── tools/
    ├── probe.py           тот, кто ходит на чужие сайты вежливо
    ├── normalize.py       превращение страницы в текст и очистка от шума
    ├── robots.py          чтение правил, которые сайт выставляет роботам
    ├── channels.py        сбор каналов Telegram и ВКонтакте
    ├── diffing.py         построчное сравнение двух снимков
    ├── prices.py          отдельный разбор чисел со страниц, где есть цены
    ├── classify.py        решение «срочно или до понедельника»
    ├── followup.py        дочитывание новости по ссылке перед отправкой
    ├── telegram.py        отправка сообщений боту радара
    ├── wording.py         как укоротить чужую длинную строку и не оборвать слово
    ├── health.py          здоровье сбора: идёт ли он вообще и что сказать, если нет
    ├── console.py         защита вывода от падения на своих же знаках
    ├── schedule.ps1       установка ежедневного запуска в планировщик Windows
    ├── calibrate.py       калибровка на живых данных: порог, шумодав, источники
    ├── calibrate_check.py проверка расчётов калибровки на выдуманных находках
    ├── noise_check.py     замер фонового шума: обоснование порога
    ├── notify_check.py    проверка правил срочности на настоящих снимках
    ├── schedule_check.py  проверка тревоги и выбора дня сводки на выдуманных днях
    ├── seed_baseline.py   точка отсчёта из страниц, сохранённых в Фазе 0
    ├── check_vk.py        проверка, что доступ к ВКонтакте живой
    └── stage_*.py         программы проверки источников
```

## Как запускать

**Всё сразу.** Главная команда: она делает то же, что расписание делает каждый
день — сбор, разбор, уведомления, по понедельникам недельную сводку, а в конце
проверку, идёт ли сбор вообще:

```
python daily.py
```

Её и запускает планировщик задач Windows раз в сутки в 11:37. Поставить,
проверить и снять расписание — одной командой каждое, порядок и ограничения в
[RASPISANIE.md](RASPISANIE.md):

```
powershell -ExecutionPolicy Bypass -File tools\schedule.ps1 -Action install
```

Остальные способы запуска:

```
python daily.py --health      показать здоровье сбора, ничего не запуская
python daily.py --dry-run     прогнать всю цепочку вхолостую
python daily.py --no-digest   без недельной сводки
```

Дальше — те же программы по отдельности. Они никуда не делись: расписание
запускает ровно их, и запуск руками ничем не отличается.

**Ежедневный сбор.** Первый шаг цепочки:

```
python collect.py
```

Она обходит все страницы и каналы, складывает снимки в `snapshots/` и пишет
отчёт `runs/<дата>.json`. Полный обход занимает около трёх минут. Всё, что
требует внимания человека, программа выводит отдельным списком в конце.

Остальные способы запуска нужны редко:

```
python collect.py --only rechka.ai   только один сайт (ключ можно повторять)
python collect.py --pages            только сайты, без соцсетей
python collect.py --channels         только соцсети
python collect.py --no-vk            пропустить ВКонтакте
python collect.py --dry-run          обойти и показать, но ничего не сохранять
python collect.py --prune-only       только вычистить снимки старше 90 дней
```

Прогон по одному сайту не стирает отчёт за день: результаты дополняют друг
друга.

**Поиск изменений.** Второй шаг, сразу после сбора:

```
python detect.py
```

Программа сравнивает свежие снимки с предыдущими и пишет найденное в
`diffs/`: подробную разницу по каждому источнику и сводку дня. На экран
выводится то же самое коротко — что изменилось, что оказалось мелочью и что
сегодня проверить не вышло.

Остальные способы запуска:

```
python detect.py --date 2026-08-19   разобрать конкретный день
python detect.py --all               пересчитать всю историю снимков
python detect.py --only rechka.ai    только один домен (ключ можно повторять)
python detect.py --full              показать все строки изменения, а не первые восемь
python detect.py --dry-run           показать, но ничего не записывать
```

**Уведомления.** Запускается сразу после поиска изменений. Смотрит, есть ли
среди находок срочное, и если есть — дочитывает новость по ссылке и отправляет
в Telegram в тот же час:

```
python notify.py
```

Недельная сводка — отдельной командой, по понедельникам. В неё попадает всё
обычное, накопившееся за неделю: под каждым пунктом видно, какие строки на
странице появились, а какие исчезли. Ниже — мелкие появления и исчезновения,
не дотянувшие до порога, строка «ничего не менялось у …», источники, которые
не собираются до сих пор, и те двое, кого приходится смотреть глазами:

```
python notify.py --digest
```

Пока бот не настроен, обе команды показывают сообщение на экране и честно
говорят, что не отправили. Как завести бота — в
[TELEGRAM-BOT.md](TELEGRAM-BOT.md).

Пока идёт неделя обкатки (`calibration.mode: true` в [config.yaml](config.yaml)),
оба сообщения уходят с шапкой «ОБКАТКА · это калибровка, а не боевая рассылка» —
и, если в настройках указан чат обкатки, туда, а не в боевой. Само наблюдение при
этом не меняется ни на букву: неделя должна измерять тот радар, который потом
заработает. Что делать в эту неделю — в [OBKATKA.md](OBKATKA.md).

Остальные способы запуска:

```
python notify.py --check             проверить, что бот на связи
python notify.py --date 2026-08-19   разобрать конкретный день
python notify.py --dry-run           показать сообщение, ничего не слать
python notify.py --resend            отправить заново, забыв про журнал
python notify.py --to 123456789      отправить в другой чат, например личный
python notify.py --digest --days 14  сводка за другой срок
```

Перед отправкой срочного радар открывает саму новость по ссылке из ленты и
кладёт в сообщение её начало: дату, с которой у конкурента новые цены, и сами
новые цены. Это два лишних запроса к чужому сайту, поэтому делается только для
срочного и не больше двух новостей за прогон — настраивается в `config.yaml`
(`follow_links`). Не дочиталось — сообщение всё равно уходит.

Одно и то же изменение дважды не присылается: программа помнит отправленное в
`notify/journal.json`. Коды возврата для расписания Фазы 5: `0` — всё хорошо,
`1` — отправить не удалось, `2` — бот не настроен, а слать было что.

**Проверка правил срочности.** Отвечает на вопрос «что человек получит, если
конкурент поднимет цену». Берёт настоящие снимки, вносит в них по одной правке —
цена, тарифный блок, заголовок первого экрана — и прогоняет через тот же
детектор и тот же классификатор, которыми работает боевой радар:

```
python tools/notify_check.py
```

**Проверка расписания и тревоги.** Отвечает на вопрос «что человек получит,
если радар перестанет собирать». Ждать двух суток без сбора ради этого нельзя,
поэтому дни выдуманные, а правила настоящие: программа создаёт отчёты прогонов
такого же вида, какой пишет сборщик, и прогоняет через них восемнадцать
положений дел — от «сегодня всё собралось» до «ноутбук был выключен трое суток»:

```
python tools/schedule_check.py
```

**Калибровка на живых данных.** Главная команда Фазы 6. Отвечает на вопрос
«прав ли был радар» — тем единственным способом, которым на него можно ответить:
человек говорит про каждую находку одно слово, а программа считает, что из этих
слов следует для порога, шумодава и списка источников.

```
python tools/calibrate.py --razmetka   завести или дополнить лист разметки
python tools/calibrate.py              отчёт: порог, шумодав, источники, двойники
```

Первая команда складывает находки недели в `calibration/razmetka.yaml`, где
против каждой надо поставить `сигнал` или `шум`. Вторая по этим словам показывает
таблицу «что дошло бы до человека при разных порогах», строки-кандидаты в
шумодав, источники, давшие только шум, и пары «одна новость двумя каналами».
Уже проставленные слова первая команда не трогает, так что запускать её можно
хоть каждый день.

Решений программа не принимает — она их готовит. Порядок недели и чем её
закрыть — в [OBKATKA.md](OBKATKA.md).

```
python tools/calibrate.py --days 14    взять другой срок
python tools/calibrate.py --lines      показать строки каждой находки
python tools/calibrate.py --dry-run    ничего не записывать
```

**Проверка калибровки.** Отвечает на вопрос «а правильно ли считает та
программа, по которой мы будем менять порог». Находки выдуманные, разбор
настоящий: сигнал ниже порога, шум выше порога, цена в восемь символов, мигающая
строка, один пост двумя каналами — пятнадцать случаев, каких за два дня
наблюдений не встретилось:

```
python tools/calibrate_check.py
```

**Замер фонового шума.** Отвечает на вопрос «а не слишком ли низко стоит
порог». Берёт страницы, снятые в Фазе 0 дважды за день, и показывает, сколько
символов детектор считает изменившимися там, где заведомо ничего не менялось:

```
python tools/noise_check.py
```

**Точка отсчёта из данных разведки.** Делается один раз, уже сделано 19.08.2026.
Превращает страницы, сохранённые в Фазе 0, в снимки за тот день, чтобы первое
сравнение случилось сразу, а не через сутки:

```
python tools/seed_baseline.py --write
```

**Проверка источников.** Она уже сделана. Повторять нужно примерно раз в квартал,
когда конкуренты перестраивают сайты и старые адреса перестают работать:

```
python tools/stage_a_home.py
python tools/stage_b_pages.py
python tools/stage_c_verify.py
python tools/stage_d_build_yaml.py
```

**ВКонтакте.** Доступ к нему живёт один час, поэтому перед каждым обходом его
надо обновить. Человек для этого не нужен, программа делает это сама:

```
python vk_token.py refresh
python vk_token.py test
```

Вторая команда показывает, от чьего имени работает доступ и сколько постов в
каждом из четырёх отслеживаемых сообществ. Отвечает — значит, всё в порядке.

## Про секреты

В проекте два доступа, и оба лежат в файлах, закрытых от git. Разница между
ними в цене ошибки: утёкший доступ к ВКонтакте позволяет писать от вашего лица,
утёкший доступ к боту — только слать сообщения в чат радара.

| Что | Где лежит | Если утёк |
|---|---|---|
| ВКонтакте | `vk_tokens.json` или `VK_TOKEN` | Отозвать и получить заново — [VK-TOKEN.md](VK-TOKEN.md) |
| Бот Telegram | `telegram_tokens.json` или `TELEGRAM_BOT_TOKEN` | Перевыпустить у @BotFather — [TELEGRAM-BOT.md](TELEGRAM-BOT.md) |

Доступ к ВКонтакте от вашего имени даёт **один-единственный файл** —
`vk_tokens.json`. Кто его получит, тот сможет читать и писать в ВКонтакте от
вашего лица, пока доступ не отозван. Больше нигде в проекте этого доступа нет:
программы `vk_token.py` и `tools/check_vk.py` только читают его из этого файла,
внутри них пусто. В `config.yaml` секретов нет и не появится — там почтовый ящик
и подпись бота.

Файл закрыт от git — системы, которая хранит все версии проекта. Закрыт списком
[.gitignore](.gitignore), и важен порядок: список составлен раньше, чем git вообще
появился в папке. Причина простая — git помнит всё, что в него попало, навсегда.
Если файл с доступом попадёт туда хотя бы один раз, удалить его следующим
действием мало: он останется в истории, и лекарство одно — отозвать доступ и
получить новый.

Что защита при этом **не** закрывает — разобрано в
[FAZA-1-OTCHET.md](FAZA-1-OTCHET.md). Если коротко: от копирования папки целиком
она не спасает, поэтому наружу отдают репозиторий, а не папку.

Получить доступ заново, если старый испортился, — одна команда, порядок расписан
в [VK-TOKEN.md](VK-TOKEN.md):

```
python vk_token.py auth <номер приложения>
```

## Как бот ведёт себя на чужих сайтах

На каждом запросе он представляется:

```
RadarBot/0.1 (+https://okk-ai.ru; competitive monitoring; contact: example@gmail.com)
```

Плюс пауза две секунды между обращениями к одному сайту, соблюдение правил,
которые сайт выставляет для роботов в файле `robots.txt`, ожидание ответа не
дольше 20 секунд и две попытки при сбое.

Это не формальность. Мы смотрим страницы, открытые всем, и не создаём сайту
нагрузку, а у его владельца должна быть возможность написать нам вместо того,
чтобы просто заблокировать. Отсюда два следствия, и оба надо принять заранее:

- **Ящик из подписи должен реально читаться.** Жалоба, оставшаяся без ответа,
  превращается в блокировку. Поменять ящик — одна строка `contact_email`
  в [config.yaml](config.yaml).
- **Конкуренты узнают, что мы за ними смотрим.** Подпись честная и называет
  компанию. Это сознательный размен, он разобран в
  [FAZA-0-OTCHET.md](FAZA-0-OTCHET.md).

## Что придётся смотреть глазами

Три источника автоматически взять не вышло. Это проверено на практике, а не
предположение:

- **aistudio.yandex.ru** — на всех страницах стоит защита от роботов, картинка с
  проверкой. Раз в месяц открывать руками. Продукт в закрытом тестировании,
  публично меняется редко.
- **Канал Roistat в Дзене** — страница отдаёт пустую заготовку, текста в ней ноль,
  ленты у Дзена нет. Раз в месяц открывать руками. Ставить ради одного канала
  тяжёлую программу-браузер — размен не в нашу пользу.
- **call-intellect.ru** — сайт не отвечает вообще никому, проверено с двух разных
  интернет-подключений. Игрок помечен как мёртвый и из обхода исключён, но из
  списка не удалён: иначе через полгода кто-нибудь начнёт разбираться заново.

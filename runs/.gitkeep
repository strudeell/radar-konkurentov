# Результат прогона: runs/<ГГГГ-ММ-ДД>.json — статус по каждому источнику.
# Папка версионируется, содержимое — нет (см. .gitignore).
# Падение одного источника не роняет прогон: смотреть статусы здесь.
